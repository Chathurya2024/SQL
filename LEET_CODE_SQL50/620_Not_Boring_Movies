# Not Boring Movies
# SOLUTION
SELECT *
FROM cinema 
WHERE mod(id,2) != 0 AND description != "boring" 
ORDER BY rating desc;

#HINT:
#STEP 1 -  Keep only rows where id is odd and description is not "boring."
#STEP 2 -  Sort Rows in descending order.
#STEP 3 -  Return all columns of the filtered and sorted rows.
